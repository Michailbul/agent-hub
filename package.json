{
  "name": "@laniameda/agent-hub",
  "version": "0.2.0",
  "main": "dist/server.js",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.3",
    "concurrently": "^9.2.1",
    "playwright": "^1.58.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "bin": {
    "agent-hub": "dist/cli.js"
  },
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "tsup src/cli.ts src/server.ts --format cjs --target node18 --watch & node dist/cli.js --no-open",
    "client:dev": "cd client && npm run dev",
    "build": "tsup src/cli.ts src/server.ts --format cjs --target node18 --clean && cd client && npm run build",
    "start": "node dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "description": "Self-hosted web editor for AI agent workspaces — SOUL.md, skills, MEMORY.md and more",
  "license": "MIT",
  "keywords": [
    "ai",
    "agents",
    "claude",
    "openai",
    "codex",
    "openclaw",
    "editor",
    "self-hosted"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Michailbul/agent-hub"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist/",
    "index.html",
    "README.md",
    "agent-hub.config.example.json",
    "docker-compose.example.yml"
  ]
}
